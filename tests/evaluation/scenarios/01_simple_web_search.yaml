id: "01_simple_web_search"
category: "tool_usage"
description: "Simple factual question requiring instant answer lookup"

channel_name: "general"
channel_description: "General discussion channel"

conversation:
  - timestamp: "14:32:15"
    user: "Alice"
    user_id: "111111111"
    content: "@SmarterDev what's the capital of France?"

recent_search_queries: []
messages_remaining: 50
is_continuation: false

expected_behavior:
  expected_tools: ["send_message", "lookup_fact"]
  expected_sequence: ["send_message", "lookup_fact", "send_message"]
  expected_response_type: "quick_casual_answer"
  description: "Should acknowledge, look up fact, then respond casually with 'Paris'"
  key_points:
    - "Sends acknowledgment before looking up"
    - "Uses lookup_fact for general fact (capital city)"
    - "Writes casual answer itself (not generate_in_depth_response)"
    - "Answer is concise and friendly"

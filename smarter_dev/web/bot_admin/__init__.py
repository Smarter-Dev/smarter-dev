"""Skrift-native replacements for the legacy /bot-admin pages.

Each module here is a focused Litestar controller under ``/admin/bot`` that
ports one feature from the legacy ``smarter_dev.web.admin`` package.
"""

apiVersion: batch/v1
kind: Job
metadata:
  name: smarter-dev-skrift-migration
  namespace: smarter-dev
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: skrift-migration
        image: zzmmrmn/smarter-dev-website:<IMAGE_VERSION>
        command: ["uv", "run", "skrift", "db", "upgrade", "heads"]
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: smarter-dev-secrets
              key: database-url
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: smarter-dev-secrets
              key: secret-key
        - name: SKRIFT_ENV
          value: "production"
  backoffLimit: 3

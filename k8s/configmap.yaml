---
apiVersion: v1
kind: ConfigMap
metadata:
  name: smarter-dev-config
  namespace: smarter-dev
data:
  # Environment configuration
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  
  # Web server configuration
  WEB_HOST: "0.0.0.0"
  WEB_PORT: "8000"
  
  # Skrift CMS environment
  SKRIFT_ENV: "production"

  # Discord OAuth redirect URI
  DISCORD_REDIRECT_URI: "https://smarter.dev/bot-admin/auth/discord/callback"
  
  # Rate limiting configuration
  RATE_LIMIT_ENABLED: "true"
  RATE_LIMIT_REQUESTS: "100"
  RATE_LIMIT_WINDOW: "60"
  
  # Bot API configuration
  API_BASE_URL: "http://smarter-dev-website:8000/api"

  # Site base URL
  SITE_BASE_URL: "https://smarter.dev"

  # LLM Model Configuration. Luna is served through OpenRouter (2026-08-06):
  # the same OpenAI upstream at half the rate. The LLM_* vars feed litellm
  # (dspy), which wants the "openrouter/<upstream>/<model>" form; the agent
  # vars are catalog wire ids.
  LLM_FAST_MODEL: "openrouter/openai/gpt-5.6-luna"
  LLM_MEDIUM_MODEL: "openrouter/openai/gpt-5.6-luna"
  LLM_JUDGE_MODEL: "openrouter/openai/gpt-5.6-luna"
  CHAT_AGENT_MODEL: "openai/gpt-5.6-luna"
  CHAT_AGENT_COMPACT_MODEL: "openai/gpt-5.6-luna"
  HANDLER_AGENT_MODEL: "openai/gpt-5.6-luna"

  # Discord voice message configuration
  VOICE_TTS_MODEL: "gemini-2.5-flash-preview-tts"
  VOICE_TTS_VOICE: "Kore"
  VOICE_TTS_SAMPLE_RATE: "24000"
  VOICE_TTS_CHANNELS: "1"
  VOICE_TTS_SAMPLE_WIDTH: "2"
  VOICE_MAX_INPUT_CHARS: "800"
  VOICE_WORDS_PER_MINUTE: "150"
  VOICE_MAX_DURATION_SECONDS: "30"
  VOICE_OPUS_BITRATE: "48k"
